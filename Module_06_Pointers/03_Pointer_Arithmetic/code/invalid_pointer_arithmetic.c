#include <stdio.h>

int main()
{
    int a = 10;
    int b = 20;
    int *p1 = &a;
    int *p2 = &b;

    // Invalid operations (do NOT uncomment)
    // p1 + p2;
    // p1 * 2;
    // p1 / 2;

    printf("Invalid pointer arithmetic is commented out\n");

    return 0;
}
